{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "zeroConfig": true
      }
    }
  ],
  "scripts": {
    "build": "npm run vercel-build"
  }
}
